# Django and REST Framework
Django>=4.2,<5.0
djangorestframework>=3.14,<4.0
django-cors-headers>=4.3,<5.0

# WebSocket support
channels>=4.0,<5.0
daphne>=4.0,<5.0

# SSH support (for system monitor)
paramiko>=3.4,<4.0

# Other utilities
Pillow>=10.0,<11.0
python-dotenv>=1.0,<2.0

# Ansible CLI used by the batch execution page
ansible-core==2.16.14
